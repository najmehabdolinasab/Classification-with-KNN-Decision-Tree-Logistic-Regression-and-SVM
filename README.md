# Classification-with-KNN-Decision-Tree-Logistic-Regression-and-SVM
A classification project that applies multiple algorithms such as KNN, Decision Tree, Logistic Regression, and SVM to predict target variables. The models are evaluated using accuracy, confusion matrix, and classification report.
